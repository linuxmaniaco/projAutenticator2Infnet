#spring.application.name=projAutenticator2Infnet
#spring.datasource.url=jdbc:mysql://localhost:3306/USUARIOS
#spring.datasource.username=root
#spring.datasource.password=123456
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/USUARIOS
    url: jdbc:mysql://mysql-server:3306/USUARIOS
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: Autenticator-service
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect # Use o dialeto correto para MySQL
  sql:
    init:
      mode: never

server:
  port: 9997

jwt:
  secret: super-secret